# 更新日志

版本号采用 `主版本.新功能.缺陷修复.小改动` 四段累计规则。

## 2.1.0.0 - 2026-09-12

### 用户体验

- 优化 LVGL 输入处理和后台模型轮询顺序，减少触摸操作被后台同步延迟的情况。
- 跳过设备卡、滑杆和下拉框的重复状态更新，降低家庭模型同步期间的无效重绘。
- 补充常见设备控制项的中文显示，底层 MIoT 协议值保持不变。
- 板端米家客户端增加 mbedTLS HTTPS 传输和 GTS Root R1 证书校验，默认通过
  `https://mi.lladlam.top` 访问公网服务。

## 2.0.0.0 - 2026-08-18

### 新功能

- 主动智能云端服务安全加固：保险箱密码强制字母+数字组合，vault 与会话元数据
  改为原子写入并收紧 0600 权限；`MIJIA_ALLOW_REMOTE_LOGIN` 现在真正生效，
  远程登录按策略默认拦截。
- 板端状态云分析任务增加超时自愈与预算回滚：in-flight 标记超时自动复位，
  失败的 LLM 调用退还已预扣的每日额度，避免上游抖动耗尽全屋配额。
- 增量同步长轮询复用近期快照，降低对米家上游 API 的请求压力；云端决策分析
  的紧急判定只信任服务端 reason，客户端无法绕过最小间隔。
- 家庭模型增量合并改用 worker 常驻缓冲并消除全量 memcmp 扫描，减少嵌入式
  堆碎片与每次模型更新的 118 KiB 全量比较。

### 缺陷修复

- 修复云端分析/学习入参校验过严导致功能永久不可用：accepted/rejected 计数
  不再被误判为 `EINVAL`；routine 置信度 clamp 到 100%，UI 不再显示 110%。
- 修复增量同步 `base_revision` 不匹配时永久停摆，现自动回退全量重同步。
- 修复登录换代后 `command_busy`/`agent_busy`/`agent_learning_busy`/
  `board_agent_busy` 无法释放导致设备指令永久 `-EBUSY`。
- 修复开关控件命令在途时状态回弹，改为乐观更新，仅失败才回滚。
- 修复家庭设备/房间/场景数量与 UI 显示不一致，统一按实际解析数为准。
- 修复登录二维码关闭后 115 KiB 缓存不释放；网络探测 id 回绕到 0 时误匹配。
- 服务端：`/api/health` 收敛返回字段；`/api/app` 第三方代理改为可配置并限制
  响应大小；请求体增加 256 KiB 上限；nonce/deviceId 改用 CSPRNG；字符串属性
  限制 192 字节；损坏 miot-spec 缓存不再导致 500。
- 服务端：场景执行前检查家庭敏感设备（摄像头/门锁/猫眼）并记录审计日志，
  可通过策略强制拒绝；会话缓存定期清理过期会话；会话锁增加超时保护。
- 服务端：登录接口回传 URL 强制校验 Xiaomi 域名，拦截钓鱼链接；清理 2300 行
  旧 Web 控制台死代码，移除无敏感过滤的历史攻击面。

### 小改动

- 主动智能控制器初始化现于启动早期调用；云端反馈队列改为全队去重。
- 启动时提示反向代理部署的 IP 合并风险，帮助公网部署正确配置可信代理头。
- 增加真实设备驱动的主动智能端到端验证（15 台真实米家设备 + 真实 LLM 分析）。

## 1.12.15.8 - 2026-07-31

### 新功能

- 增加板端主动智能核心，以统一时间和米家设备状态作为上下文，学习通用时间习惯
  与设备行为关联并展示触发依据、建议动作和置信度；睡眠准备仅作为冷启动示例。
- 扩展主动智能事件输入，所有已同步的布尔属性、数值属性和设备上下线变化均可
  触发安全的可写属性建议；数值状态按区间匹配，同一天的独立行为可持续积累证据。
- 增加 HPR4 到 HPR5 的主动智能画像迁移，扩展规则和近期事件容量时保留已有学习
  结果；过滤板端自身控制产生的回显，避免形成自激循环。
- 增加约 25 秒的七天模拟历史回放和第八天现场反馈演示；演示画像与真实画像
  隔离，回放期间不修改正式用户数据。
- 增加“执行建议”“仅今天忽略”“以后少提醒”和“重置画像”交互；设备操作
  必须由用户确认，执行时复用现有 MIoT 属性控制与真实状态确认链路。
- 增加主动智能画像的 `userid` 独立双槽持久化，按自然日抑制重复建议并实际执行
  提醒冷却策略。
- 增加纯 C 主机测试，覆盖冷启动、历史回放、反馈学习、演示数据隔离、画像恢复、
  无可控灯具和跨日冷却行为。
- 增加可选云端深度分析顾问；只上传匿名聚合情境，云端只能在本地许可策略中
  建议、暂缓或继续观察，最终目标绑定和执行确认仍由板端完成。

### 缺陷修复

- 修复云端分析异常挂起时界面可能长期停留在“分析中”的问题，板端等待 20 秒
  后恢复本地算法，断网时立即忽略旧云端建议。
- 修复云端中文文本按字符而非 UTF-8 字节截断、非法 UTF-8 进入字体渲染路径，
  以及决策与动作策略组合不一致的问题。
- 修复主动智能快照和云端请求在 UI 约 5 ms 主循环中被高频轮询，改为分级节流，
  避免与触摸、LVGL 刷新和米家模型锁竞争。
- 修复 GMAC 首次 PHY 协商未完成或链路工作排队失败后载波轮询永久停止的问题；
  watchdog 现在独立续约并自动重试，MDIO 与工作队列错误采用限频日志。家庭屏首次
  启动增加 12 秒协商宽限期，已建立过链路后的真实断线仍立即提示。
- 增加 RTL8201F 无链路恢复：清除 PHY 掉电/隔离状态、重新启动自动协商，并输出
  BMCR、BMSR、本地通告和链路伙伴能力，支持插线后无需重启系统自动恢复。

### 小改动

- 将米家模型、主动智能和网络状态检查改为分级节流及原位更新，减少无变化轮询、
  重复页面重建和串口日志对触摸主循环的干扰。
- 补充 `1.12.15.8` 主机测试、服务端回归和开发板冒烟报告，并按大赛官方
  AI Coding 日志格式重新导出、自动脱敏和校验本次完整 Codex 会话。

## 1.11.15.8 - 2026-07-27

### 新功能

- 增加房间二级导航、可滚动设备列表和房间内 MIoT 设备控制。
- 增加按设备能力动态生成的详情页：传感器显示温湿度和电量，灯具、音响、插座
  等设备按实际 MIoT 属性提供开关、亮度、色温、音量和目标温度控制。
- 增加服务端快照缓存和带连续版本的 MIoT 增量同步，仅向板端传递变化属性。
- 增加覆盖 MiSans 全部字符的 1 bpp LVGL BinFont 分区和动态字形加载。

### 缺陷修复

- 修复家庭与房间页面反复切换后的卡死、非法指令和触摸延迟。
- 修复 RISC-V 浮点上下文初始化、保存与恢复不完整导致的中断异常。
- 修复 TinyTTF 运行时栅格化与 LVGL 内存不足导致的启动和数据渲染崩溃。
- 修复登录持久化记录的 Flash 访问边界与掉电一致性问题。
- 修复 LVGL 按钮使用空 transition 描述符后，在状态变化时触发 Load access fault。
- 修复设备详情页温度、湿度、电量和控件值收到增量后不实时更新的问题。
- 修复同名属性可能路由到错误 MIoT 实例的问题，控制请求优先使用 `siid/piid`。
- 修复房间页面对象过多、频繁全页重建和过密模型轮询造成的触摸延迟。

### 小改动

- 降低网络健康检查频率和无变化同步日志量，减少后台任务对 UI 的干扰。
- 调整 16 MiB SPI NOR 的系统、字体和数据分区打包流程。
- 增加驱动、稳定性、证据归档和 XTS 的完整验收矩阵与测试记录模板。

## 1.7.7.5 - 2026-07-16

### 新功能

- 增加板端米家登录持久化，重启后可从 Data 分区恢复服务令牌和保险箱解锁信息。
- 增加家庭、房间、设备、场景和属性的完整同步及本地模型，支持设备状态增量更新。
- 增加 MIoT 属性设置、Action 和场景执行闭环，控制结果由后续真实状态同步确认。
- 增加 NTP 时间同步和 UTC+8 本地时间显示。

### 缺陷修复

- 修复家庭数据同步后触摸延迟、页面切换卡顿和重复整页解析渲染的问题。
- 修复家庭与房间页面往返时的非法指令异常，并消除中断路径中的浮点上下文风险。
- 修复已有版本标签指向旧提交时 Release job 直接失败的问题；已发布版本现在安全跳过，
  未发布的孤立标签仍会阻止覆盖。

## 1.3.4.5 - 2026-07-15

### 新功能

- 将 NuttX 执行区扩展到 8 MiB PSRAM，并同步调整 tinySPL 镜像装载范围、链接脚本
  和运行时堆边界，为家庭屏网络与图形功能留出稳定空间。
- 家庭屏增加米家 App 扫码登录流程，可请求一次性会话、显示 RGB565 二维码、
  领取短期令牌，并展示家庭名称、设备总数与在线数。
- 增加独立的 `mijia-api` 服务端二维码传输与 Xiaomi Passport 长轮询登录链路；
  生产实例使用 `https://mi.lladlam.top`，账号保险库不进入开发板固件。

### 小改动

- 增加新电脑完整编译指南，覆盖 Ubuntu/Debian 与 Arch Linux 环境。
- PR 合并后直接按 `VERSION` 创建标签和 GitHub Release，无需再次手工推送标签。
- 补充家庭屏服务端、开源项目、许可证和构建产物说明。

## 1.2.4.4 - 2026-07-15

### 小改动

- Release job 显式指定 GitHub 仓库，使独立检出目录下的 `gh release`
  命令可以在固件构建成功后创建发布。

## 1.2.4.3 - 2026-07-15

### 新功能

- 家庭屏自动检测网线、DHCP 地址、DNS 与互联网连通状态。
- 家庭屏随板级初始化自动启动，无需从 NSH 手动运行。

### 缺陷修复

- 修复 D13x GMAC 接收帧与 DHCP 应答处理，完成有线网络联通。
- 修复系统定时器和任务优先级导致网络初始化无法继续的问题。
- 修复 GT911 空样本使 LVGL 输入轮询无法退出、界面不刷新的问题。
- 使用未压缩 MiSans 字形并补齐网络文案，恢复完整中文显示。

### 小改动

- 调整家庭屏与网络初始化线程优先级。
- 清除启动、网络和定时器临时诊断标记。
- 更新家庭屏、网络验证和第三方组件说明。
