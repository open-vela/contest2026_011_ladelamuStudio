# 更新日志

版本号采用 `主版本.新功能.缺陷修复.小改动` 四段累计规则。

## 1.10.11.7 - 2026-07-19

### 新功能

- 增加房间二级导航、可滚动设备列表和房间内 MIoT 设备控制。
- 增加服务端快照缓存和带连续版本的 MIoT 增量同步，仅向板端传递变化属性。
- 增加覆盖 MiSans 全部字符的 1 bpp LVGL BinFont 分区和动态字形加载。

### 缺陷修复

- 修复家庭与房间页面反复切换后的卡死、非法指令和触摸延迟。
- 修复 RISC-V 浮点上下文初始化、保存与恢复不完整导致的中断异常。
- 修复 TinyTTF 运行时栅格化与 LVGL 内存不足导致的启动和数据渲染崩溃。
- 修复登录持久化记录的 Flash 访问边界与掉电一致性问题。

### 小改动

- 降低网络健康检查频率和无变化同步日志量，减少后台任务对 UI 的干扰。
- 调整 16 MiB SPI NOR 的系统、字体和数据分区打包流程。

## 1.7.7.5 - 2026-07-16

### 新功能

- 增加板端米家登录持久化，重启后可从 Data 分区恢复服务令牌和保险箱解锁信息。
- 增加家庭、房间、设备、场景和属性的完整同步及本地模型，支持设备状态增量更新。
- 增加 MIoT 属性设置、Action 和场景执行闭环，控制结果由后续真实状态同步确认。
- 增加 NTP 时间同步和 UTC+8 本地时间显示。

### 缺陷修复

- 修复家庭数据同步后触摸延迟、页面切换卡顿和重复整页解析渲染的问题。
- 修复家庭与房间页面往返时的非法指令异常，并消除中断路径中的浮点上下文风险。
- 修复已有版本标签指向旧提交时 Release job 直接失败的问题；已发布版本现在安全跳过，
  未发布的孤立标签仍会阻止覆盖。

## 1.3.4.5 - 2026-07-15

### 新功能

- 将 NuttX 执行区扩展到 8 MiB PSRAM，并同步调整 tinySPL 镜像装载范围、链接脚本
  和运行时堆边界，为家庭屏网络与图形功能留出稳定空间。
- 家庭屏增加米家 App 扫码登录流程，可请求一次性会话、显示 RGB565 二维码、
  领取短期令牌，并展示家庭名称、设备总数与在线数。
- 增加独立的 `mijia-api` 服务端二维码传输与 Xiaomi Passport 长轮询登录链路；
  生产实例使用 `https://mi.lladlam.top`，账号保险库不进入开发板固件。

### 小改动

- 增加新电脑完整编译指南，覆盖 Ubuntu/Debian 与 Arch Linux 环境。
- PR 合并后直接按 `VERSION` 创建标签和 GitHub Release，无需再次手工推送标签。
- 补充家庭屏服务端、开源项目、许可证和构建产物说明。

## 1.2.4.4 - 2026-07-15

### 小改动

- Release job 显式指定 GitHub 仓库，使独立检出目录下的 `gh release`
  命令可以在固件构建成功后创建发布。

## 1.2.4.3 - 2026-07-15

### 新功能

- 家庭屏自动检测网线、DHCP 地址、DNS 与互联网连通状态。
- 家庭屏随板级初始化自动启动，无需从 NSH 手动运行。

### 缺陷修复

- 修复 D13x GMAC 接收帧与 DHCP 应答处理，完成有线网络联通。
- 修复系统定时器和任务优先级导致网络初始化无法继续的问题。
- 修复 GT911 空样本使 LVGL 输入轮询无法退出、界面不刷新的问题。
- 使用未压缩 MiSans 字形并补齐网络文案，恢复完整中文显示。

### 小改动

- 调整家庭屏与网络初始化线程优先级。
- 清除启动、网络和定时器临时诊断标记。
- 更新家庭屏、网络验证和第三方组件说明。
